# Production base URL — used for canonical URLs, sitemap, robots.txt, OG tags.
NEXT_PUBLIC_SITE_URL=https://guidancecyber.com

# Future: contact form handler (currently mailto:). Replace before launch.
# CONTACT_FORM_ENDPOINT=https://example.com/api/contact
